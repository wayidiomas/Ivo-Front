# IVO Frontend - Configurações de Produção
# Copie este arquivo para .env.production no Render

# URL do backend (será gerada automaticamente pelo Render)
BACKEND_URL=https://seu-backend-render.onrender.com

# URL do frontend (será gerada automaticamente pelo Render)  
FRONTEND_URL=https://seu-frontend-render.onrender.com

# IMPORTANTE: Esta variável deve ser IGUAL à BACKEND_URL
# Usada para bypass em desenvolvimento (não funcionará em produção por segurança)
NEXT_PUBLIC_BACKEND_URL=https://seu-backend-render.onrender.com

# Modo de produção
NODE_ENV=production

# Configurações adicionais para Render
# PORT será definido automaticamente pelo Render, mas pode ser útil para testes locais de produção
# PORT=3000